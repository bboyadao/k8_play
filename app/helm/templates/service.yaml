apiVersion: v1
kind: Service
metadata:
  name: {{ include "chuthe.fullname" . }}
  labels:
    {{- include "chuthe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chuthe.selectorLabels" . | nindent 4 }}
